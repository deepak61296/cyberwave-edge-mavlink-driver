[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cyberwave-edge-mavlink-driver"
version = "0.1.0"
description = "MAVLink edge driver for ArduPilot and PX4 aircraft (SITL and real). Speaks the standard Cyberwave drone command contract."
requires-python = ">=3.10"
dependencies = [
    "cyberwave>=0.7.0",
    "pymavlink>=2.4.40",
]
authors = [
    {name = "Deepak Popli"},
]

[project.scripts]
driver = "cyberwave_edge_mavlink_driver.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["cyberwave_edge_mavlink_driver*"]

[tool.pytest.ini_options]
# tests/ also holds live flight scripts that move a real aircraft at
# import time, so only test_unit_*.py may be collected.
testpaths = ["tests"]
python_files = ["test_unit_*.py"]
